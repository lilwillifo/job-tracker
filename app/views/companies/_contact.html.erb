
    <th>Edit</th>
    <th>Delete</th>
    <th>Name</th>
    <th>Role</th>
    <th>E-mail</th>

    <ul class=contact_<%= contact.id %>>
      <%= link_to 'Delete', company_contact_path(contact.company, contact), method: :delete %></ul>
      <ul><%= contact.name %></ul>
      <ul><%= contact.role %></ul>
      <ul><%= contact.email %></ul>
